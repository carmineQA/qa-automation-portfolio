# ai/

This folder is a human-readable mirror of the Claude Code configuration under `.claude/` (agents, skills, MCP references), kept here because `.claude/` is a hidden directory that most file browsers and GitHub's default view won't surface. `ai/skills/` and `.claude/skills/` are kept in sync manually; `.claude/` remains the source of truth actually used by Claude Code.
